import React from "react";
import SuperButton from "./SuperButton/SuperButton";

export const Checkboxs = () => {
    return (
        <>
            CheckBox's:
            <SuperButton>
                default
            </SuperButton>
        </>
    );
};